<article class="card">
  <div class:list={"card-heading"}>
    <div class="card-heading-top">
      <slot name="card-heading" />
      <div class="external-link-icons">
        <slot name="external-link-icons" />
      </div>
    </div>
    <slot name="card-subheading" />
    <slot name="card-subsubheading" />
  </div>
  <div class="card-content">
    <slot name="card-content" />
  </div>
</article>

<style>
  .card {
    border: 2px solid var(--primaryColor);
    transition: 0.5s all;
  }

  .card:hover {
    border: 2px solid var(--accentColor);
  }

  .icon {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .card-heading {
    padding: 1.2rem;
  }
  .card-heading-top {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  }
  .card-content {
    padding: 1.2rem;
  }
</style>
